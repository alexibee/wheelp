<% if message.user == current_user %>
  <div class="message-cont d-flex justify-content-end">
    <div class="your-message single-message" id="message-<%= message.id %>">
        <small>
          <strong><%= message.user.first_name %></strong>
          <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
        </small>
        <p><%= message.content %></p>
    </div>
  </div>
<% else %>
  <div class="message-cont d-flex justify-content-start">
    <div class="not-your-message single-message" id="message-<%= message.id %>">
        <small>
          <strong><%= message.user.first_name %></strong>
          <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
        </small>
        <p><%= message.content %></p>
    </div>
  </div>
<% end %>
